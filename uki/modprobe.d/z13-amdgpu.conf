# AMDGPU module parameters for ThinkPad Z13 Gen 1.
#
# This file is packed into the UKI addon initrd so it is available before the
# upstream KDE Linux initramfs loads amdgpu via the KMS hook.
options amdgpu abmlevel=0 aspm=1 bapm=1 dpm=1
